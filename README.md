# Simulador-TCP-BBR
Simulador sirve para comprender el funcionamiento básico del algoritmo TCP BBR. Permite observar cómo, a partir del ancho de banda y el RTT, se calcula el BDP y se ajusta la ventana de congestión y la velocidad de transmisión. 
